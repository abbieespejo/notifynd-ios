import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'notifynd.ionic.starter',
  appName: 'notifynd-ios-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
